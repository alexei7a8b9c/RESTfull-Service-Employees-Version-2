### Base URL
@baseUrl = http://localhost:8080

### 1. ТЕСТЫ АВТОРИЗАЦИИ

### Тест 1.1: Успешный логин ADMIN
POST {{baseUrl}}/api/auth/login
Content-Type: application/json

{
  "username": "admin",
  "password": "admin123"
}

> {%
    client.test("ADMIN login successful", function() {
        client.assert(response.status === 200, "Status should be 200");
        client.assert(response.body.success === true, "Success should be true");
        client.assert(response.body.data.username === "admin", "Username should be admin");
        client.assert(response.body.data.role === "ADMIN", "Role should be ADMIN");
        client.assert(response.body.data.token !== null, "Token should be present");

        client.global.set("admin_token", response.body.data.token);
        client.log("🔑 ADMIN token: " + client.global.get("admin_token"));
    });
%}

### Тест 1.2: Успешный логин USER
POST {{baseUrl}}/api/auth/login
Content-Type: application/json

{
  "username": "user",
  "password": "user123"
}

> {%
    client.test("USER login successful", function() {
        client.assert(response.status === 200, "Status should be 200");
        client.assert(response.body.success === true, "Success should be true");
        client.assert(response.body.data.username === "user", "Username should be user");
        client.assert(response.body.data.role === "USER", "Role should be USER");
        client.assert(response.body.data.token !== null, "Token should be present");

        client.global.set("user_token", response.body.data.token);
        client.log("🔑 USER token: " + client.global.get("user_token"));
    });
%}

### Тест 1.3: Неверный пароль ADMIN
POST {{baseUrl}}/api/auth/login
Content-Type: application/json

{
  "username": "admin",
  "password": "wrongpassword"
}

> {%
    client.test("ADMIN wrong password", function() {
        client.assert(response.status === 404 || response.status === 401, "Should be 404 or 401 for wrong password");
    });
%}

### Тест 1.4: Несуществующий пользователь
POST {{baseUrl}}/api/auth/login
Content-Type: application/json

{
  "username": "nonexistent",
  "password": "password"
}

> {%
    client.test("Non-existent user", function() {
        client.assert(response.status === 404, "Should be 404 for non-existent user");
    });
%}

### Тест 1.5: Доступ без токена
GET {{baseUrl}}/api/employees

> {%
    client.test("Access without token", function() {
        client.assert(response.status === 401, "Should be 401 Unauthorized without token");
    });
%}

### Тест 1.6: Невалидный токен
GET {{baseUrl}}/api/employees
Authorization: Bearer invalid_token_12345

> {%
    client.test("Invalid token", function() {
        client.assert(response.status === 401, "Should be 401 for invalid token");
    });
%}

### 2. ТЕСТЫ СОЗДАНИЯ (CREATE)

### Тест 2.1: ADMIN создает сотрудника
POST {{baseUrl}}/api/employees
Authorization: Bearer {{admin_token}}
Content-Type: application/json

{
  "firstName": "Иван",
  "lastName": "Петров",
  "email": "ivan.petrov@company.com",
  "phone": "+79991234567",
  "status": "ACTIVE",
  "hireDate": "2024-01-15",
  "departmentIds": [1, 2]
}

> {%
    client.test("ADMIN can create employee", function() {
        client.assert(response.status === 200, "ADMIN should be able to create employee");
        client.assert(response.body.data.firstName === "Иван", "First name should match");
        client.assert(response.body.data.lastName === "Петров", "Last name should match");
        client.assert(response.body.data.status === "ACTIVE", "Status should be ACTIVE");

        client.global.set("created_employee_id", response.body.data.id);
        client.log("👤 Created employee ID: " + client.global.get("created_employee_id"));
    });
%}

### Тест 2.2: USER пытается создать сотрудника
POST {{baseUrl}}/api/employees
Authorization: Bearer {{user_token}}
Content-Type: application/json

{
  "firstName": "Пользователь",
  "lastName": "Тестовый",
  "email": "user.test@company.com",
  "status": "ACTIVE"
}

> {%
    client.test("USER cannot create employee", function() {
        client.assert(response.status === 403, "USER should get 403 Forbidden for create");
    });
%}

### Тест 2.3: ADMIN создает отдел
POST {{baseUrl}}/api/departments
Authorization: Bearer {{admin_token}}
Content-Type: application/json

{
  "name": "Тестовый отдел",
  "description": "Описание тестового отдела"
}

> {%
    client.test("ADMIN can create department", function() {
        client.assert(response.status === 200, "ADMIN should be able to create department");
        client.assert(response.body.data.name === "Тестовый отдел", "Department name should match");

        client.global.set("created_department_id", response.body.data.id);
        client.log("🏢 Created department ID: " + client.global.get("created_department_id"));
    });
%}

### Тест 2.4: USER пытается создать отдел
POST {{baseUrl}}/api/departments
Authorization: Bearer {{user_token}}
Content-Type: application/json

{
  "name": "Отдел пользователя",
  "description": "Создан пользователем"
}

> {%
    client.test("USER cannot create department", function() {
        client.assert(response.status === 403, "USER should get 403 Forbidden for create department");
    });
%}

### 3. ТЕСТЫ ЧТЕНИЯ (READ)

### Тест 3.1: ADMIN получает всех сотрудников
GET {{baseUrl}}/api/employees
Authorization: Bearer {{admin_token}}

> {%
    client.test("ADMIN can read all employees", function() {
        client.assert(response.status === 200, "ADMIN should be able to read employees");
        client.assert(Array.isArray(response.body.data), "Response should be array");
        client.log("📋 ADMIN retrieved " + response.body.data.length + " employees");
    });
%}

### Тест 3.2: USER получает всех сотрудников
GET {{baseUrl}}/api/employees
Authorization: Bearer {{user_token}}

> {%
    client.test("USER can read all employees", function() {
        client.assert(response.status === 200, "USER should be able to read employees");
        client.assert(Array.isArray(response.body.data), "Response should be array");
        client.log("📋 USER retrieved " + response.body.data.length + " employees");
    });
%}

### Тест 3.3: ADMIN получает сотрудника по ID
GET {{baseUrl}}/api/employees/{{created_employee_id}}
Authorization: Bearer {{admin_token}}

> {%
    client.test("ADMIN can read employee by ID", function() {
        client.assert(response.status === 200, "ADMIN should be able to read employee by ID");
        client.assert(response.body.data.id === client.global.get("created_employee_id"), "Employee ID should match");
    });
%}

### Тест 3.4: USER получает сотрудника по ID
GET {{baseUrl}}/api/employees/{{created_employee_id}}
Authorization: Bearer {{user_token}}

> {%
    client.test("USER can read employee by ID", function() {
        client.assert(response.status === 200, "USER should be able to read employee by ID");
        client.assert(response.body.data.id === client.global.get("created_employee_id"), "Employee ID should match");
    });
%}

### Тест 3.5: ADMIN получает все отделы
GET {{baseUrl}}/api/departments
Authorization: Bearer {{admin_token}}

> {%
    client.test("ADMIN can read all departments", function() {
        client.assert(response.status === 200, "ADMIN should be able to read departments");
        client.assert(Array.isArray(response.body.data), "Response should be array");
        client.log("📋 ADMIN retrieved " + response.body.data.length + " departments");
    });
%}

### Тест 3.6: USER получает все отделы
GET {{baseUrl}}/api/departments
Authorization: Bearer {{user_token}}

> {%
    client.test("USER can read all departments", function() {
        client.assert(response.status === 200, "USER should be able to read departments");
        client.assert(Array.isArray(response.body.data), "Response should be array");
        client.log("📋 USER retrieved " + response.body.data.length + " departments");
    });
%}

### 4. ТЕСТЫ ОБНОВЛЕНИЯ (UPDATE)

### Тест 4.1: ADMIN обновляет сотрудника
PUT {{baseUrl}}/api/employees/{{created_employee_id}}
Authorization: Bearer {{admin_token}}
Content-Type: application/json

{
  "firstName": "Иван Обновленный",
  "lastName": "Петров",
  "email": "ivan.updated@company.com",
  "phone": "+79991234568",
  "status": "ON_LEAVE",
  "hireDate": "2024-01-15",
  "departmentIds": [1]
}

> {%
    client.test("ADMIN can update employee", function() {
        client.assert(response.status === 200, "ADMIN should be able to update employee");
        client.assert(response.body.data.firstName === "Иван Обновленный", "First name should be updated");
        client.assert(response.body.data.email === "ivan.updated@company.com", "Email should be updated");
        client.assert(response.body.data.status === "ON_LEAVE", "Status should be updated to ON_LEAVE");
    });
%}

### Тест 4.2: USER пытается обновить сотрудника
PUT {{baseUrl}}/api/employees/{{created_employee_id}}
Authorization: Bearer {{user_token}}
Content-Type: application/json

{
  "firstName": "Пользователь Обновил",
  "lastName": "Сотрудника",
  "status": "ACTIVE"
}

> {%
    client.test("USER cannot update employee", function() {
        client.assert(response.status === 403, "USER should get 403 Forbidden for update");
    });
%}

### Тест 4.3: ADMIN обновляет отдел
PUT {{baseUrl}}/api/departments/{{created_department_id}}
Authorization: Bearer {{admin_token}}
Content-Type: application/json

{
  "name": "Обновленный отдел",
  "description": "Обновленное описание отдела"
}

> {%
    client.test("ADMIN can update department", function() {
        client.assert(response.status === 200, "ADMIN should be able to update department");
        client.assert(response.body.data.name === "Обновленный отдел", "Department name should be updated");
    });
%}

### Тест 4.4: ADMIN обновляет фото сотрудника
PATCH {{baseUrl}}/api/employees/{{created_employee_id}}/photo?photoUrl=https://company.com/photos/ivan.jpg
Authorization: Bearer {{admin_token}}

> {%
    client.test("ADMIN can update employee photo", function() {
        client.assert(response.status === 200, "ADMIN should be able to update photo");
    });
%}

### 5. ТЕСТЫ УДАЛЕНИЯ (DELETE)

### Тест 5.1: ADMIN удаляет сотрудника (soft delete)
DELETE {{baseUrl}}/api/employees/{{created_employee_id}}
Authorization: Bearer {{admin_token}}

> {%
    client.test("ADMIN can delete employee", function() {
        client.assert(response.status === 200, "ADMIN should be able to delete employee");
        client.log("🗑️ Employee soft deleted successfully");
    });
%}

### Тест 5.2: USER пытается удалить сотрудника
DELETE {{baseUrl}}/api/employees/1
Authorization: Bearer {{user_token}}

> {%
    client.test("USER cannot delete employee", function() {
        client.assert(response.status === 403, "USER should get 403 Forbidden for delete");
    });
%}

### Тест 5.3: ADMIN удаляет отдел (soft delete)
DELETE {{baseUrl}}/api/departments/{{created_department_id}}
Authorization: Bearer {{admin_token}}

> {%
    client.test("ADMIN can delete department", function() {
        client.assert(response.status === 200, "ADMIN should be able to delete department");
        client.log("🗑️ Department soft deleted successfully");
    });
%}

### Тест 5.4: Проверка soft delete - сотрудник не найден
GET {{baseUrl}}/api/employees/{{created_employee_id}}
Authorization: Bearer {{admin_token}}

> {%
    client.test("Soft deleted employee not found", function() {
        client.assert(response.status === 404, "Soft deleted employee should return 404");
    });
%}

### 6. ТЕСТЫ ПОИСКА И ФИЛЬТРАЦИИ

### Тест 6.1: ADMIN поиск сотрудников по имени
POST {{baseUrl}}/api/employees/search
Authorization: Bearer {{admin_token}}
Content-Type: application/json

{
  "firstName": "John",
  "page": 0,
  "size": 10,
  "sortBy": "firstName",
  "sortDirection": "ASC"
}

> {%
    client.test("ADMIN can search employees by name", function() {
        client.assert(response.status === 200, "ADMIN should be able to search employees");
        client.assert(response.body.data.content !== undefined, "Response should have content field");
        client.log("🔍 ADMIN search found " + response.body.data.content.length + " employees");
    });
%}

### Тест 6.2: USER поиск сотрудников по статусу
POST {{baseUrl}}/api/employees/search
Authorization: Bearer {{user_token}}
Content-Type: application/json

{
  "status": "ACTIVE",
  "page": 0,
  "size": 5,
  "sortBy": "lastName",
  "sortDirection": "ASC"
}

> {%
    client.test("USER can search employees by status", function() {
        client.assert(response.status === 200, "USER should be able to search employees");
        client.assert(response.body.data.content !== undefined, "Response should have content field");
        client.log("🔍 USER search found " + response.body.data.content.length + " active employees");
    });
%}

### Тест 6.3: ADMIN поиск по нескольким отделам
POST {{baseUrl}}/api/employees/search
Authorization: Bearer {{admin_token}}
Content-Type: application/json

{
  "departmentIds": [1, 2],
  "page": 0,
  "size": 20
}

> {%
    client.test("ADMIN can search by multiple departments", function() {
        client.assert(response.status === 200, "ADMIN should be able to search by departments");
    });
%}

### Тест 6.4: USER поиск с пагинацией
POST {{baseUrl}}/api/employees/search
Authorization: Bearer {{user_token}}
Content-Type: application/json

{
  "page": 0,
  "size": 2,
  "sortBy": "hireDate",
  "sortDirection": "DESC"
}

> {%
    client.test("USER can search with pagination", function() {
        client.assert(response.status === 200, "USER should be able to search with pagination");
        if (response.body.data.content) {
            client.log("📄 Pagination: page " + response.body.data.number +
                ", size " + response.body.data.size +
                ", total " + response.body.data.totalElements);
        }
    });
%}

### Тест 6.5: ADMIN поиск по менеджеру
POST {{baseUrl}}/api/employees/search
Authorization: Bearer {{admin_token}}
Content-Type: application/json

{
  "managerName": "John",
  "page": 0,
  "size": 10
}

> {%
    client.test("ADMIN can search by manager name", function() {
        client.assert(response.status === 200, "ADMIN should be able to search by manager");
    });
%}

### 7. ТЕСТЫ ВАЛИДАЦИИ И ОШИБОК

### Тест 7.1: Создание сотрудника с невалидными данными
POST {{baseUrl}}/api/employees
Authorization: Bearer {{admin_token}}
Content-Type: application/json

{
  "firstName": "",
  "lastName": "",
  "email": "invalid-email",
  "status": "INVALID_STATUS"
}

> {%
    client.test("Validation errors for invalid employee data", function() {
        client.assert(response.status === 400, "Should get 400 for invalid data");
    });
%}

### Тест 7.2: Создание отдела без имени
POST {{baseUrl}}/api/departments
Authorization: Bearer {{admin_token}}
Content-Type: application/json

{
  "name": "",
  "description": "Отдел без имени"
}

> {%
    client.test("Validation errors for invalid department data", function() {
        client.assert(response.status === 400, "Should get 400 for invalid department data");
    });
%}

### Тест 7.3: Получение несуществующего сотрудника
GET {{baseUrl}}/api/employees/99999
Authorization: Bearer {{admin_token}}

> {%
    client.test("Non-existent employee returns 404", function() {
        client.assert(response.status === 404, "Should get 404 for non-existent employee");
    });
%}

### Тест 7.4: Обновление несуществующего сотрудника
PUT {{baseUrl}}/api/employees/99999
Authorization: Bearer {{admin_token}}
Content-Type: application/json

{
  "firstName": "Несуществующий",
  "lastName": "Сотрудник",
  "status": "ACTIVE"
}

> {%
    client.test("Update non-existent employee returns 404", function() {
        client.assert(response.status === 404, "Should get 404 for non-existent employee update");
    });
%}

### 8. ФИНАЛЬНЫЕ ПРОВЕРКИ

### Тест 8.1: Создание тестовых данных для демонстрации
POST {{baseUrl}}/api/employees
Authorization: Bearer {{admin_token}}
Content-Type: application/json

{
  "firstName": "Демо",
  "lastName": "Пользователь",
  "email": "demo.user@company.com",
  "phone": "+79998887766",
  "status": "ACTIVE",
  "hireDate": "2024-03-01",
  "departmentIds": [1, 3]
}

> {%
    client.global.set("demo_employee_id", response.body.data.id);
%}

### Тест 8.2: Итоговая проверка системы
GET {{baseUrl}}/api/employees
Authorization: Bearer {{admin_token}}

> {%
    client.test("Final system check", function() {
        client.assert(response.status === 200, "System is working correctly");
        client.log(" ");
        client.log("🎉 ВСЕ ТЕСТЫ УСПЕШНО ЗАВЕРШЕНЫ!");
        client.log("✅ Аутентификация работает корректно");
        client.log("✅ Ролевая модель соблюдается:");
        client.log("   - ADMIN: CREATE, READ, UPDATE, DELETE");
        client.log("   - USER: только READ");
        client.log("✅ CRUD операции работают правильно");
        client.log("✅ Поиск и фильтрация функционируют");
        client.log("✅ Валидация данных работает");
        client.log("✅ Soft delete корректно реализован");
        client.log(" ");
        client.log("🚀 Система готова к использованию!");
    });
%}